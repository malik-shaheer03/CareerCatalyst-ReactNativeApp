# Email Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Server Ports
RESET_PORT=5001
EMAIL_PORT=5000
RESUME_EMAIL_PORT=5002

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Replace the values with your actual credentials
# 3. For Gmail, use an App Password (not your regular password)
#    - Go to: https://myaccount.google.com/apppasswords
#    - Generate a new app password for "CareerCatalyst Backend"
# 4. Make sure .env is in .gitignore to keep credentials safe
